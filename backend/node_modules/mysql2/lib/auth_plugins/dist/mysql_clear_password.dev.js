'use strict';

function bufferFromStr(str) {
  return Buffer.from("".concat(str, "\0"));
}

var create_mysql_clear_password_plugin = function create_mysql_clear_password_plugin(pluginOptions) {
  return function mysql_clear_password_plugin(_ref) {
    var connection = _ref.connection,
        command = _ref.command;
    var password = command.password || pluginOptions.password || connection.config.password;
    return function ()
    /* pluginData */
    {
      return bufferFromStr(password);
    };
  };
};

module.exports = create_mysql_clear_password_plugin;